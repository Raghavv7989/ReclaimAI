"""Composite match scoring engine."""

# TODO: Implement MatchScorer class with:
# - compute_image_score(emb_a, emb_b) -> float
# - compute_text_score(emb_a, emb_b) -> float
# - compute_location_score(loc_a, loc_b) -> float
# - compute_time_score(time_a, time_b) -> float
# - compute_composite_score(scores) -> float
